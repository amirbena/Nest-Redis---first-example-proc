export enum Role {
    MODERATOR = 0,
    USER = 1,
    ADMIN = 2,
    SUPER_ADMIN = 3
}

export const RoleString={
      0: "MODERATOR",
      1: "USER",
      2: "ADMIN",
      3: "SUPER_ADMIN"
}

export enum TABLE_NAMES {
    USERS = "users",
    CATEGORIES = "categories",
    PRODUCTS = "products"
}
